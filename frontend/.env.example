# CAIP-Karnataka Frontend — Environment Variables
# Copy to .env for local (non-Docker) `npm run dev`.
# Must always include the full backend origin + /api/v1 path — a
# relative path here is exactly the port-mismatch bug that broke login.
VITE_API_BASE_URL=http://localhost:8000/api/v1
